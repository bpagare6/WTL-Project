let app,models;_fec‍.w("./index",[["default",["app"],function(v){app=v}]]);_fec‍.w("./models/index",[["default",["models"],function(v){models=v}]]);


async function run() {
  try {
    // DB Connection
    await models.sequelize.sync();
    console.log("Database Connected successfully.");

    // Start the App in Listen Mode
    // If PORT is declared as environment variable then use that or use 3000
    const port = process.env.PORT || 3000;
    app.listen(port, () => {
      console.log(`Server is up and listening at port ${port} ...`);
    });
  } catch (e) {
    console.log("Error while initializing.");
    _fec‍.g.console.log(e);
  }
}

run();
